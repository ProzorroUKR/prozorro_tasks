<?xml version="1.0" encoding="windows-1251"?>
<root method_name="PrContract">
    <contract>
        <contractId>{{ contract.id }}</contractId>
        <contractNumber>{{ contract.contractNumber }}</contractNumber>
        <contractsPeriodStartDate>{{ contract.period.startDate }}</contractsPeriodStartDate>
        <contractsPeriodEndDate>{{ contract.period.endDate }}</contractsPeriodEndDate>
        <contractsValueAmount>{{ contract.value.amount }}</contractsValueAmount>
        <contractsValueCurrency>{{ contract.value.currency }}</contractsValueCurrency>
        <contractsValueAmountNet>{{ contract.value.amountNet }}</contractsValueAmountNet>
        <contractsDateSigned>{{ contract.dateSigned }}</contractsDateSigned>
        <contractsDocuments>{{ contract.documents }}</contractsDocuments>
    </contract>
    <changes>{% for change in contract.changes %}
        <change>
            <changeId>{{ change.id }}</changeId>
            <contractNumber>{{ contract.contractNumber }}</contractNumber>
            <changeRationale>{{ change.rationale }}</changeRationale>
            <changeRationaleTypes>{{ change.rationaleTypes|join(', ') }}</changeRationaleTypes>
            <contractsDateSigned>{{ change.dateSigned }}</contractsDateSigned>
            <changeDocuments>{{ change.documents }}</changeDocuments>
        </change>{% endfor %}</changes>
    {% if plan %}{% include 'plan.xml' %}{% endif %}
{% include 'report.xml' %}
</root>